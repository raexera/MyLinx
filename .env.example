# ==============================================================================
# MyLinx - Docker Environment Variables
# Copy this to .env in the project root (NOT the Laravel .env inside /src)
# ==============================================================================

# Host UID/GID (auto-detected by Makefile, override here if needed)
# UID=1000
# GID=1000
